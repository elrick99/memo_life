/// Radius scale ported from `--radius: 1rem` (16px) and its `-2px`/`-4px`
/// derivations in `resources/css/app.css`.
class AppRadius {
  const AppRadius._();

  static const sm = 12.0;
  static const md = 14.0;
  static const lg = 16.0;

  /// The admin hero banner and larger cards use an even softer corner.
  static const xl = 28.0;
}
